import styles from "./styles.module.scss";

const Media = () => {
  return (
    <div>
      Media Container - For times sake, I will continue on to the next project,
      and come back to this to continue the track list and media buttons!
    </div>
  );
};

export default Media;
